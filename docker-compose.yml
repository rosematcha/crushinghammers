services:
  bot:
    build: .
    restart: unless-stopped
    environment:
      DISCORD_TOKEN: ${DISCORD_TOKEN}
      GUILD_ID: ${GUILD_ID}
      HAMMERS_DB: /data/hammers.db
    volumes:
      - hammers-data:/data

volumes:
  hammers-data:
